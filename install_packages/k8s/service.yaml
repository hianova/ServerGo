apiVersion: v1
kind: Service
metadata:
  name: servergo-service
  namespace: servergo
spec:
  selector:
    app: servergo
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
